Sources for the plugin `plugin.appsflyer.strict`.


Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appsflyer.strict" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
Note: Source Code is in `plugin.appsflyer` repo
